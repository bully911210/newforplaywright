# ══════════════════════════════════════════════════════════════════════
# MMX Automation — Environment Configuration
# ══════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ══════════════════════════════════════════════════════════════════════

# ── MMX Login Credentials ────────────────────────────────────────────
# Your MMX Systems (mmxsystems.co.za) username and password.
MMX_USERNAME=your_username
MMX_PASSWORD="your_password"
MMX_BASE_URL=https://www.mmxsystems.co.za

# ── Google Sheet API ─────────────────────────────────────────────────
# Deploy code.gs as a Google Apps Script Web App and paste the URL here.
# The script provides read/write access to the sheet data.
GOOGLE_SHEET_WEBAPP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec

# Column-to-field mapping (matches your Google Sheet header layout).
# Column A = status flag (managed by the automation), B-S = client data fields.
COLUMN_MAPPING={"A":"status","B":"clientName","C":"clientSurname","D":"idNumber","E":"cellphone","F":"email","G":"address","H":"city","I":"province","J":"postalCode","K":"accountHolder","L":"bank","M":"accountNumber","N":"accountType","O":"contractAmount","P":"paymentFrequency","Q":"debitOrderDate","R":"dateSaleMade","S":"agent"}

# ── Browser Settings ─────────────────────────────────────────────────
# HEADLESS=true  → no visible browser (production)
# HEADLESS=false → visible browser window (debugging)
HEADLESS=false

# Persistent browser profile directory. Login sessions are preserved
# across restarts. Change if the profile becomes corrupted.
USER_DATA_DIR=user-data-6

# Timeouts (milliseconds) for page navigation and individual actions.
NAVIGATION_TIMEOUT=30000
ACTION_TIMEOUT=10000

# ── Polling ──────────────────────────────────────────────────────────
# How often (ms) the standalone poller checks the sheet for new rows.
POLL_INTERVAL_MS=30000

# AUTO_START_POLLING controls whether the MCP server auto-polls.
# The standalone poller (npm start) ALWAYS polls regardless of this flag.
# Set to false to prevent the MCP server from launching its own poller.
AUTO_START_POLLING=false

# ── Dashboard ────────────────────────────────────────────────────────
# Port for the real-time monitoring dashboard (standalone mode only).
DASHBOARD_PORT=3000
